import anime from "animejs";
var showSkills = 0
const mokaTopTableGrid =[3,10];



/*
function IntroSkillAnimationListener(){

    var skills = document.querySelector('#skills');
    var stupid= skills.getBoundingClientRect();
    var skillsPosTop = stupid.top;

    var intermediate = document.querySelector('#Intermediate');
    var stupid2= intermediate.getBoundingClientRect();
    var  intermediatePosX= stupid2.x;
    
    var beginner = document.querySelector('#Beginner');
    var stupid3 = beginner.getBoundingClientRect();
    var  beginnerPosX= stupid3.x;

  

    var intermediatePercentage = intermediatePosX*0.01;
    var intermediatePoint=intermediatePosX-(intermediatePercentage*30);
    console.log('INTERMEDIATE POS X: '+intermediatePosX);

    console.log('BEGINNER POS X: '+beginnerPosX);

    //intermediatePosX = intermediatePosX+(intermediatePercentage*7);
    console.log('skills: '+skillsPosTop);

    if(skillsPosTop<160 && showSkills === 0){

        
     tl.play();
     //
      }

    }
     document.addEventListener('scroll',IntroSkillAnimationListener);
       

  //  }
//});

*/
